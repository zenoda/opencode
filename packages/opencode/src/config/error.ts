export * as ConfigError from "./error"

import { InvalidError, JsonError, FrontmatterError, DirectoryTypoError } from "@opencode-ai/core/v1/config/error"

export { InvalidError, JsonError, FrontmatterError, DirectoryTypoError }
